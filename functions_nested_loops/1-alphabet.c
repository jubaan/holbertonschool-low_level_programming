#include "main.h"

/**
 * main - Entry point
 *
 * Description: This program prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int letter = 'a';

	while (letter < 'z')
	{
		_putchar(letter);
		i++;
	}
	_putchar(letter);
	return (0);
}

