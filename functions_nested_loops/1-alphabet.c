#include "main.h"

/**
 * main - Entry point
 *
 * Description: This program prints the alphabet in lowercase
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char letter = 'a';

	while (letter < 'z')
	{
		_putchar(letter);
		a++;
	}
	_putchar(letter);
	return (0);
}

