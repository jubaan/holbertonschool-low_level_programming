#include "main.h"

/**
 * _abs - Function computes the absolute value of an integer
 * @n: integer to compute the absolute value
 *
 * Return: Alwas 0 (Success0)
 */

int _abs(int n)
{
	if (n < 0)
		return (-n);
	return (n);
}

